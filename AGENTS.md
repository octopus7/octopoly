# AGENTS.md — OctoPoly rebuild rules

이 파일은 저장소 전체의 Codex 작업에 적용한다.

## 현재 기준

- `main`은 OctoPoly를 처음부터 다시 쌓기 위한 최소 정적 Cloudflare Pages 기준선이다.
- 초기화 전 구현은 `codex/pre-minimal-main-20260811` 브랜치에 보존되어 있다.
- 보존 브랜치의 코드는 사용자가 요청한 기능에 필요한 부분만 검토해 선택적으로 가져온다.
- 과거 계획, 작업 분배, 진행률 기록을 현재 구현 지침으로 간주하지 않는다.

## 작업 방식

- 한 번에 하나의 작은 기능 단위를 완성하고 검증한 뒤 커밋한다.
- 기본은 주 에이전트가 순차적으로 작업한다.
- 서브에이전트는 사용자가 요청했거나, 파일 소유가 겹치지 않는 명백히 독립적인 조사·검증이 실질적인
  이득을 줄 때만 제한적으로 사용한다. 리드타임 단축만을 위한 극단적인 병렬화는 하지 않는다.
- 아직 요청되지 않은 후속 기능을 미리 구현하지 않는다.
- 기존 코드를 가져올 때는 현재 요구사항에 맞는지 다시 검토하고, 불필요한 결합이나 과거 구조를 함께
  복원하지 않는다.

## 제품 및 배포

- 제품 표시 이름은 `OctoPoly`다.
- Cloudflare Pages 프로젝트는 `octopoly`, production URL은 `https://octopoly.pages.dev/`이다.
- 현재 앱은 정적 Pages SPA다. 승인 없이 Pages Functions, Workers 또는 bindings를 추가하지 않는다.
- primary target은 iPad Safari지만, 기능별 입력 장치와 브라우저 지원 범위는 구현 시 명시한다.

## 구현 및 검증

- 변경 범위를 작게 유지하고 관련 테스트, 타입 검사, 빌드를 수행한다.
- 검증하지 않은 동작을 완료로 표현하지 않는다.
- 새로운 래스터 이미지나 기존 래스터 편집이 실제로 필요하면 ImageGen을 사용한다. SVG, HTML/CSS,
  canvas 또는 코드 기반 시각 요소가 더 적합하면 ImageGen을 억지로 사용하지 않는다.
- 기능 단위가 완료되면 관련 변경만 커밋한다. 사용자의 별도 지시 없이 다른 기능을 같은 커밋에 섞지 않는다.
