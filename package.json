{
  "name": "octopoly",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.18.0",
    "npm": "11.18.0"
  },
  "packageManager": "npm@11.18.0",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && vite build",
    "ci": "npm run typecheck && npm run test && vite build && node scripts/verify-baseline.mjs dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "22.18.0",
    "jsdom": "29.1.1",
    "typescript": "7.0.2",
    "vite": "8.2.1",
    "vitest": "4.1.10"
  }
}
